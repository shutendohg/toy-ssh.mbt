///|
/// Self-built crypto primitives for the toy SSH implementation:
/// Poly1305, X25519, Ed25519 (see docs/03). SHA-256, SHA-512, HMAC and
/// ChaCha20 are reused from moonbitlang/x/crypto.
///
/// Toy code: not constant-time, not hardened. Never use for real security.
