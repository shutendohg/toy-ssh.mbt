// toy-ssh: an experimental, educational SSH client + server in MoonBit.
// Toy. Never use for real security.

name = "shutendohg/toy-ssh"

version = "0.1.0"

source = "src"

repository = "https://github.com/shutendohg/toy-ssh.mbt"

license = "MIT"

preferred_target = "native"

description = "Toy SSH client and server in MoonBit (educational, not secure)"

import {
  "moonbitlang/x@0.5.5",
  "moonbitlang/async@0.21.3",
}
